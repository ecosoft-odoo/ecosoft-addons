<?xml version="1.0" encoding="utf-8" ?>
<odoo>
    <record id="view_account_debit_note" model="ir.ui.view">
        <field name="name">account.debit.note.form</field>
        <field name="model">account.debit.note</field>
        <field name="inherit_id" ref="account_debit_note.view_account_debit_note" />
        <field name="arch" type="xml">
            <xpath expr="//field[@name='reason']" position="before">
                <field name="etax_status" invisible="1" />
                <field
                    name="debit_note_doctype_code_ids"
                    widget="many2many_tags"
                    readonly="1"
                />
                <field
                    name="purpose_code_id"
                    domain="[('etax_doctype_code_ids', 'in', debit_note_doctype_code_ids)]"
                    invisible="etax_status != 'success'"
                    required="etax_status == 'success'"
                    options="{'no_open':True}"
                />
            </xpath>
        </field>
    </record>
</odoo>
