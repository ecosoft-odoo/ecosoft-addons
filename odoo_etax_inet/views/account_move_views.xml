<?xml version="1.0" encoding="utf-8" ?>
<odoo>

    <!--
        decoration-info
        decoration-success
        decoration-warning
        decoration-muted
        decoration-danger
     -->
    <record id="view_move_form_etax" model="ir.ui.view">
        <field name="name">view.move.form</field>
        <field name="model">account.move</field>
        <field name="inherit_id" ref="account.view_move_form" />
        <field name="arch" type="xml">
            <xpath expr="//button[@name='button_draft']" position="after">
                <button
                    string='Sign E-Tax Invoice'
                    name="odoo_etax_inet.action_select_etax_doctype_view"
                    type="action"
                    class="btn-info"
                />
            </xpath>
            <field name="payment_reference" position="after">
                <field name="etax_doctype" />
                <field
                    name="etax_status"
                    widget="badge"
                    decoration-success="etax_status=='success'"
                    decoration-danger="etax_status=='error'"
                    decoration-warning="etax_status in ['processing', 'to_process']"
                />
                <field name="etax_transaction_code" />
                <field name="etax_error_code" />
                <field name="etax_error_message" />
            </field>
        </field>
    </record>
</odoo>
