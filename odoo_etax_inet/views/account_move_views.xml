<?xml version="1.0" encoding="utf-8" ?>
<odoo>

    <!--
        decoration-info
        decoration-success
        decoration-warning
        decoration-muted
        decoration-danger
     -->
    <record id="view_move_form_etax" model="ir.ui.view">
        <field name="name">view.move.form</field>
        <field name="model">account.move</field>
        <field name="inherit_id" ref="account.view_move_form" />
        <field name="arch" type="xml">
            <xpath expr="//header" position="after">
                <field name="etax_status" invisible="1" />
                <div
                    groups="account.group_account_invoice,account.group_account_readonly"
                    class="text-right alert alert-success"
                    role="alert"
                    attrs="{'invisible': [('etax_status', 'not in', ('success'))]}"
                >
                    Digitally signed by บริษัท อีโคซอฟต์จำกัด
                </div>
            </xpath>
            <xpath expr="//button[@name='button_draft']" position="after">
                <button
                    string='Sign E-Tax Invoice'
                    name="odoo_etax_inet.action_select_etax_doctype_view"
                    type="action"
                    class="btn-info"
                    attrs="{'invisible': ['|', ('etax_status', 'in', ('success')), ('state', 'not in', ('posted'))]}"
                />
            </xpath>
            <xpath expr="//page[@id='other_tab_entry']" position="after">
                <page id="odoo_etax_inet" string="e-Tax Info">
                    <group>
                        <field
                            name="etax_doctype"
                            string="e-Tax doctype"
                            readonly="1"
                            force_save="1"
                        />
                        <field
                            name="etax_status"
                            string="e-Tax status"
                            widget="badge"
                            decoration-success="etax_status=='success'"
                            decoration-danger="etax_status=='error'"
                            decoration-warning="etax_status in ['processing', 'to_process']"
                            readonly="1"
                            force_save="1"
                        />
                        <field
                            name="etax_transaction_code"
                            string="e-Tax transaction code"
                            readonly="1"
                            force_save="1"
                        />
                        <field
                            name="etax_error_code"
                            string="e-Tax error code"
                            readonly="1"
                            force_save="1"
                        />
                        <field
                            name="etax_error_message"
                            string="e-Tax message"
                            readonly="1"
                            force_save="1"
                        />
                    </group>
                </page>
            </xpath>
        </field>
    </record>
</odoo>
